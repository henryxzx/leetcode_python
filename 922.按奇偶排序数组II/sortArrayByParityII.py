class Solution:
    def sortArrayByParityII(self, A):
        """
        :type A: List[int]
        :rtype: List[int]
        """
        ans = []
        for i in range(len(A)):
            if i % 2 == 0:
               ans.append()
